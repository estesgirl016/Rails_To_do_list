<h1>To Do List</h1>
  <% @todos.each do |todo| %>
<h2><%= todo.title %></h2>
<h3><%= todo.completed %></h3>
<p><%= todo.body %></p>
  <% link_to 'Show todo', todo_path(todo) %>
<% end %>

<%= link_to 'New To do', new_todo_path %>
